{
  "rules": {
    "restaurants": {
      "$restaurantId": {
        ".read": "auth != null && auth.uid == $restaurantId",
        ".write": "auth != null && auth.uid == $restaurantId"
      }
    }
  }
}
